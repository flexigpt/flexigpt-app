type: agent
name: release-notes-writer
displayName: Release Notes Writer
description: Create release notes and changelogs from diffs, commit notes, pull request descriptions, issue summaries, or version notes.
labels:
  category: technical-content-writing

members:
  - type: text
    name: release-notes-writer-instructions
    insert: instructions
    parameters:
      mediaType: text/markdown
      content: |-
        Create release notes grounded in supplied source material.
        Separate added, changed, fixed, breaking changes, migration notes, known issues, and internal-only changes.
        Mark unsupported or incomplete claims as draft.

  - type: text
    name: release-notes-writer-request
    insert: user-message
    parameters:
      mediaType: text/markdown
      content: |-
        <Attach or paste diffs, PR summaries, commit notes, issue lists, or version notes for the release. Include the version if known.>

        Create release notes from this source material. Separate added, changed, fixed, breaking changes, migration notes, known issues, and internal-only changes. Mark gaps as draft.

  - type: tool
    name: statpath
    scope: builtin
    overrides:
      autoExecute: true

  - type: tool
    name: listdirectory
    scope: builtin
    overrides:
      autoExecute: true

  - type: tool
    name: searchfiles
    scope: builtin
    overrides:
      autoExecute: true

  - type: tool
    name: readfile
    scope: builtin
    overrides:
      autoExecute: true

  - type: tool
    name: readtextrange
    scope: builtin
    overrides:
      autoExecute: true

  - type: tool
    name: findtext
    scope: builtin
    overrides:
      autoExecute: true

  - type: tool
    name: writefile
    scope: builtin
    overrides:
      autoExecute: false

  - type: tool
    name: inserttext
    scope: builtin
    overrides:
      autoExecute: false

  - type: tool
    name: replacetext
    scope: builtin
    overrides:
      autoExecute: false

  - type: tool
    name: deletetext
    scope: builtin
    overrides:
      autoExecute: false
